{% extends 'base_admin.html.twig' %}

{% block body %}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Création d'une newsletter</h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>

        <!-- /.row -->
        <div class="row">
            <div class="col-md-8 col-md-offset-2">

                {{ form_start(form) }}
                <div class="row">
                    <div class="col-md-3">
                        {{ form_label(form.email) }}
                    </div>
                    <div class="col-md-9">
                        {{ form_widget(form.email) }}
                        <div class="input-group has-error">
                            {{ form_errors(form.email) }}
                        </div>
                    </div>
                </div>

                <div>&nbsp;</div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}